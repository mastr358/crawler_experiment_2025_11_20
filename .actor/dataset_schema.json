{
    "actorSpecification": 1,
    "fields": {
        "url": { "type": "string", "description": "The URL of the page" },
        "title": { "type": "string", "description": "The title of the page" },
        "content": { "type": "string", "description": "The content converted to Markdown" },
        "crawled_at": { "type": "string", "format": "date-time", "description": "Timestamp of crawl" }
    },
    "views": {
        "overview": {
            "title": "Overview",
            "transformation": {
                "fields": ["title", "url", "crawled_at"]
            },
            "display": {
                "component": "table",
                "properties": {
                    "title": { "label": "Title", "format": "text" },
                    "url": { "label": "URL", "format": "link" },
                    "crawled_at": { "label": "Crawled At", "format": "date" }
                }
            }
        }
    }
}